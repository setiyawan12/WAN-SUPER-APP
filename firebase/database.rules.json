{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        "vaults": {
          "$vaultId": {
            "items": {
              ".indexOn": ["updatedAt"]
            }
          }
        }
      }
    },
    "mindmaps": {
      "$mindmapId": {
        ".read": "auth != null && ($mindmapId.beginsWith(auth.uid + '--') || data.child('ownerId').val() === auth.uid || root.child('mindmapAccess').child($mindmapId).child(auth.uid).child('read').val() === true || root.child('groupAccess').child(data.child('ownerId').val()).child(auth.uid).child('read').val() === true || auth.token.admin === true)",
        ".write": "auth != null && ((!data.exists() && newData.child('ownerId').val() === auth.uid && newData.child('updatedBy').val() === auth.uid) || (!data.exists() && root.child('groupAccess').child(newData.child('ownerId').val()).child(auth.uid).child('write').val() === true && newData.child('updatedBy').val() === auth.uid) || (data.child('ownerId').val() === auth.uid && newData.child('ownerId').val() === data.child('ownerId').val() && newData.child('updatedBy').val() === auth.uid) || root.child('mindmapAccess').child($mindmapId).child(auth.uid).child('write').val() === true || root.child('groupAccess').child(data.child('ownerId').val()).child(auth.uid).child('write').val() === true || auth.token.admin === true)",
        "ownerId": {
          ".validate": "newData.isString() && newData.val().length <= 128"
        },
        "projectId": {
          ".validate": "newData.isString() && newData.val().length <= 500"
        },
        "revision": {
          ".validate": "newData.isNumber() && newData.val() >= 0 && (!data.exists() || newData.val() >= data.val())"
        },
        "updatedAt": {
          ".validate": "newData.isNumber()"
        },
        "updatedBy": {
          ".validate": "newData.val() === auth.uid"
        },
        "snapshot": {
          ".validate": "newData.exists() && newData.child('nextId').isNumber()"
        }
      }
    },
    "mindmapAccess": {
      "$mindmapId": {
        "$uid": {
          ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true)",
          ".write": false
        }
      }
    },
    "groupAccess": {
      "$groupId": {
        "$uid": {
          ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true)",
          ".write": false
        }
      }
    },
    "presence": {
      "$mindmapId": {
        "$uid": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid === $uid",
          ".validate": "newData.val() === null || newData.hasChildren(['updatedAt'])"
        }
      }
    }
  }
}