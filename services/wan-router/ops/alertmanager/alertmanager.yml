route:
  receiver: local-log
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-log

# Production notification channels are managed outside this repository.
# Apply them with least-privilege credentials and never commit webhook URLs.